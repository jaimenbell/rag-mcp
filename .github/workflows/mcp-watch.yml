name: MCP Watch
on:
  push:
  schedule: [{cron: "0 6 * * 1"}]
jobs:
  mcp-watch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: jaimenbell/mcp-security-scanner/action@v0.2.0
